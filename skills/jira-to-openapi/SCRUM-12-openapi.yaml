openapi: 3.0.3
info:
  title: Add PATCH /api/tasks/{id} for partial task updates
  description: Add PATCH /api/tasks/{id} for partial task updates
  version: 1.0.0
  x-jira-issue: SCRUM-12
  x-generated-at: '2026-03-12T13:13:17.959983Z'
servers:
- url: https://api.example.com/v1
  description: Production
- url: https://staging-api.example.com/v1
  description: Staging
paths:
  /api/tasks/{id}:
    post:
      summary: Add PATCH /api/tasks/{id} for partial task updates
      description: Add PATCH /api/tasks/{id} for partial task updates
      operationId: postApiTasks
      tags:
      - Task
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TaskResponse'
        '400':
          description: Bad request
        '401':
          description: Unauthorized
        '500':
          description: Internal server error
components:
  schemas:
    TaskResponse:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Unique resource identifier
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
security:
- BearerAuth: []
tags:
- name: Task
  description: Operations on Task resources
